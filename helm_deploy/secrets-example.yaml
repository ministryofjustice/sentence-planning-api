# This is a YAML-formatted file.
#
# Template for secrets which will be stored in AWS secret manager

secrets:
  API_CLIENT_CREDENTIALS_ID: sentence-plan-client-api
  API_CLIENT_CREDENTIALS_SECRET: secret123
